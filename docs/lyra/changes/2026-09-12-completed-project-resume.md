Change / date: Completed-project resume authority / 2026-09-12
User problem and reproduction: Reopening a finished Personal project could claim an unseen completion event for a task that had since been archived. The automatic coordinator turn interrupted the user's new question and used old conversation scope to ask whether the already-finished application should be considered finished.
In scope / explicitly deferred: Silently consume notifications whose current task is archived, and make the latest approved project profile, brief and saved status authoritative when a real task notification is reviewed. Do not delete conversation history, alter archived jobs, suppress current non-archived completion/block notifications, or change project scope.
Acceptance criteria: An unseen completion event for a subsequently archived task does not create a TUI notification and is not offered again. A current completed task still notifies. The coordinator envelope says not to revive superseded scope or reconfirm a finish line when the latest approved saved state is complete.
Affected modules and data: `tui_gateway/server.py`, `hermes_cli/project_job_attention.py`, and their tests. Existing notification cursors advance normally; there is no migration and no project-file mutation.
Tests and observed results: Both regressions failed before implementation and passed afterward. The full TUI gateway, saved-attention, real project-workflow, model-provider, Ultimate Builder, and Lyra release checks completed within a combined 688-test run with zero failures. The web check passed 404 tests plus typecheck and lint (31 pre-existing warnings), and the production build passed with its existing bundle-size warning.
Compatibility / restart: Existing databases remain compatible. A running Lyra process must be restarted to load the changed gateway code.
Rollback / retained recovery data: Revert the focused commit. Task events and project history remain stored; only the per-subscription notification cursor advances as before.
Local commit / authorized push: User previously authorized pushing Lyra modifications; included in the verified v0.19.36 release commit.
