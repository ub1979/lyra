# Lyra 0.19.65 beta publication

## Scope and authority

User explicitly confirmed pushing all 64 commits ahead of origin/main at
`9158f18a9`, plus the required patch release update. Publish to
`https://github.com/ub1979/lyra.git`, main; no force push or history rewrite.
Untracked local outputs, lockfiles and generated user projects remain excluded.

## Change and impact

Bump Lyra once from 0.19.64 to 0.19.65. Keep beta maturity, September 19 release
date, desktop package/lockfile, dashboard labels and changelog synchronized.
Upstream Hermes version remains unchanged. No new runtime behavior is introduced
by this release commit; regenerate maintained assets/inventory as needed.

User waived Safari/Firefox testing for this publication. Preserve the limitation
in release notes; do not represent those browsers as tested. Existing QA-only
evidence and 1,418 passing affected regression tests are documented in the
[QA retest](../2026-09-19-qa-routing-retest.md) and
[routing repair record](2026-09-19-qa-dependency-routing.md). No overall stable
release or fresh full-project acceptance is claimed.

## Verification and recovery

Release checks: 42 version/QA-control Python tests passed; Studio typecheck/build
and 495 web tests passed; web lint has zero errors and 30 existing warnings.
TUI rebuild and eight mounted startup/paste regression tests passed. Maintained
file-index check passed. These are local checks; remote CI will run after push.
Studio build regenerated the same asset content (version is fetched at runtime).
The plugin dashboard's plain-JavaScript source and distributed copy carry the
matching label. No additional Safari/Firefox or full-project journey performed.
Restart idle dashboard/coordinator processes to adopt
all accumulated changes; the QA runner was already restarted for the live test.
No live jobs are restarted as part of publication. Revert focused commits if
needed; do not reset user projects or local changes. No data migration added.
