name: disk-cleanup
version: 2.0.0
description: "Auto-track and clean up ephemeral files created during AppIT sessions. Runs via plugin hooks — no agent action required."
author: "AppIT"
hooks:
  - post_tool_call
  - on_session_end
